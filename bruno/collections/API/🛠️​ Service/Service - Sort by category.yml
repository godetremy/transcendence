info:
  name: Service - Sort by category
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/services/category/:category_id?limit=20&page=1"
  params:
    - name: limit
      value: "20"
      type: query
    - name: page
      value: "1"
      type: query
    - name: category_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
