info:
  name: Service - Get detail service
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/services/:service_id"
  params:
    - name: service_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
