info:
  name: Setup - Mark as done
  type: http
  seq: 1

http:
  method: PUT
  url: "{{BaseURL}}/setup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
