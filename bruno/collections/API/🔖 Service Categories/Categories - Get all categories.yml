info:
  name: Categories - Get all categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
