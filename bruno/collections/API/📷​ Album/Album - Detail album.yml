info:
  name: Album - Detail album
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/albums/:album_id"
  params:
    - name: album_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
