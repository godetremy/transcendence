info:
  name: Album - Lists albums (admin)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/albums"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
