info:
  name: Events - Subscribe to event
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BaseURL}}/events/:event_id"
  params:
    - name: event_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
