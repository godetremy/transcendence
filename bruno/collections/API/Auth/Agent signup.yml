info:
  name: Agent signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "test.123",
        "passwordCheck": "test.123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
