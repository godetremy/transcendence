info:
  name: Logout
  type: http
  seq: 5

http:
  method: GET
  url: "{{BaseURL}}/auth/logout"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
