info:
  name: Agent signin
  type: http
  seq: 3

http:
  method: POST
  url: "{{BaseURL}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "test.123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
