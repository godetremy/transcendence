info:
  name: Agent list approve
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/auth/approval/:id/approve"
  params:
    - name: id
      value: me
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
