info:
  name: Agent list reject
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/approval/:id/reject"
  params:
    - name: id
      value: me
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
