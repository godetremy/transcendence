info:
  name: Agent list
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/auth/approval/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
