info:
  name: 42 - Authorize
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/auth/oauth/fortytwo/authorize?code=code"
  params:
    - name: code
      value: code
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
