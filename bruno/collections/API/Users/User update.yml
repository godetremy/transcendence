info:
  name: User update
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseURL}}/users/:id"
  params:
    - name: id
      value: me
      type: path
  body:
    type: json
    data: |-
      {
        "name": "test",
        "reason": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
