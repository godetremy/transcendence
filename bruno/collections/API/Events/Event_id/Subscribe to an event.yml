info:
  name: Subscribe to an event
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/events/:event_id/subscribe"
  params:
    - name: event_id
      value: idevent
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
