info:
  name: Report an Image for a specified event
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseURL}}/events/:event_id/photos/report"
  params:
    - name: event_id
      value: idtest
      type: path
  body:
    type: json
    data: |-
      {
        "reason": "i don't like it"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
