info:
  name: Unsubscribe to an event
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/events/:event_id/unsubscribe"
  params:
    - name: event_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
