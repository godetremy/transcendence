info:
  name: Delete a specified event
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BaseURL}}/events"
  body:
    type: json
    data: |-
      {
        "event_id": "event_id"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
