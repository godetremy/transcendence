info:
  name: Modify a specified event
  type: http
  seq: 4

http:
  method: POST
  url: "{{BaseURL}}/events/:event_id"
  params:
    - name: event_id
      value: idtest
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
