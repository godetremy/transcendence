info:
  name: Create a new event
  type: http
  seq: 4

http:
  method: POST
  url: "{{BaseURL}}/events"
  body:
    type: json
    data: |-
      {
        "title": "title",
        "description": "a description",
        "max_inscription": 20,
        "start_at": "06/11/2026",
        "end_at": "06/11/2026"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
