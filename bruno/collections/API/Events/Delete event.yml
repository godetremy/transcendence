info:
  name: Delete event
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BaseURL}}/events/:event_id"
  params:
    - name: event_id
      value: idtest
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
