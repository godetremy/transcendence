info:
  name: Agent - List pending approval
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/users/approval/pending?limit=10&page=1"
  params:
    - name: limit
      value: "10"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
