info:
  name: Balance - Create transaction
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseURL}}/users/:id/balance"
  params:
    - name: id
      value: test
      type: path
  body:
    type: json
    data: |-
      {
        "description": "test",
        "amount": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
