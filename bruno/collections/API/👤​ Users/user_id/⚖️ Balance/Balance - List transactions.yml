info:
  name: Balance - List transactions
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/users/:id/balance/:balance_id"
  params:
    - name: balance_id
      value: test
      type: path
    - name: id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
