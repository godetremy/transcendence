info:
  name: User - Delete account
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BaseURL}}/users/:id/delete"
  params:
    - name: id
      value: me
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
