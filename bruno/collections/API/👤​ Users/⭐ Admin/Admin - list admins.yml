info:
  name: Admin - list admins
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/users/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
