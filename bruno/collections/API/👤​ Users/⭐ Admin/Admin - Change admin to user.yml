info:
  name: Admin - Change admin to user
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BaseURL}}/users/admin/:id"
  params:
    - name: id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
