info:
  name: Pending - list to approve agents
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/users/approval/pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
