info:
  name: Status - Agent status
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseURL}}/users/approval/:id/status"
  params:
    - name: id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
