info:
  name: Agent - Set approval
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{BaseURL}}/users/approval/:id"
  params:
    - name: id
      value: b2d948cd-9f2b-4382-b169-a00303b7f3ba
      type: path
  body:
    type: json
    data: |-
      {
        "approve": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
