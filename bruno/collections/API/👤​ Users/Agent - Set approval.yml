info:
    name: Agent - Set approval
    type: http
    seq: 5

http:
    method: PATCH
    url: '{{BaseURL}}/users/approval/:id'
    params:
        - name: id
          value: me
          type: path
    body:
        type: json
        data: |-
            {
              "approve": false
            }
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
