info:
    name: User - Delete account
    type: http
    seq: 3

http:
    method: GET
    url: '{{BaseURL}}/users/:id/delete'
    params:
        - name: id
          value: ''
          type: path
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
