info:
  name: Upload - Create request
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/upload/request"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
