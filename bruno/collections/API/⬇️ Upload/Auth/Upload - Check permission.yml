info:
  name: Upload - Check permission
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/upload/auth"
  headers:
    - name: Authorization
      value: test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
