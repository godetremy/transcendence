info:
  name: Organization - Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/organization"
  body:
    type: json
    data: |-
      {
        "name": "bde2",
        "description": "bureau des eleves",
        "club": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
