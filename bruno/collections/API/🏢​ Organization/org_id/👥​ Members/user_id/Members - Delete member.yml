info:
  name: Members - Delete member
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BaseURL}}/organization/:org_id/members/:user_id"
  params:
    - name: user_id
      value: test
      type: path
    - name: org_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
