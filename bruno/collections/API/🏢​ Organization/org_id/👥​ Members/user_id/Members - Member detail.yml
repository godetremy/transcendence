info:
  name: Members - Member detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/organization/:org_id/members/:user_id?page=1&limit=10"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
    - name: user_id
      value: test
      type: path
    - name: org_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
