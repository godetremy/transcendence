info:
  name: Followers - Get number of followers
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/organization/:org_id/followers/number"
  params:
    - name: org_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
