info:
  name: Organization - Edit organization
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BaseURL}}/organization/:org_id"
  params:
    - name: org_id
      value: test
      type: path
  body:
    type: json
    data: |-
      {
        "name": "test",
        "description": "test2",
        "logo": "test3",
        "club": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
