info:
  name: Organization - Get organization
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/organization/:org_id"
  params:
    - name: org_id
      value: test
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
