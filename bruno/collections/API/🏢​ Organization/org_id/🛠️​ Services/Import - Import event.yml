info:
  name: Import - Import event
  type: http
  seq: 4

http:
  method: POST
  url: "{{BaseURL}}/organization/:org_id/services"
  params:
    - name: org_id
      value: test
      type: path
  body:
    type: file
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
