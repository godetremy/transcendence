info:
  name: Approve - Lists Approve
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/organization/approve"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
