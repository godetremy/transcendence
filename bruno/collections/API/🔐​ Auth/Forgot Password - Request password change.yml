info:
  name: Forgot Password - Request password change
  type: http
  seq: 5

http:
  method: POST
  url: "{{BaseURL}}/auth/forgot-password"
  body:
    type: form-urlencoded
    data:
      - name: mail
        value: test@free.fr
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
