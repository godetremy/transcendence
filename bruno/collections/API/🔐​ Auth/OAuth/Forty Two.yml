info:
  name: Forty Two
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseURL}}/auth/oauth/fortytwo/authorize"
  params:
    - name: code
      value: test
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
