info:
  name: Info - Check token session
  type: http
  seq: 4

http:
  method: GET
  url: "{{BaseURL}}/auth/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
