info:
  name: Logout
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseURL}}/auth/logout"
  body:
    type: form-urlencoded
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
